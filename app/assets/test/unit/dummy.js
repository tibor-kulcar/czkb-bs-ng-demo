describe('service', function () {
    beforeEach(angular.mock.module('czkb-bs-ng-demo', function () {
        }
    ))

    it("Ensure right universe", function() {
        expect(0).toEqual(1);
    })
});
